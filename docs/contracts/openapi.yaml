openapi: 3.1.0
info:
  title: caramel API
  version: 0.0.0
  description: >-
    Authoritative contract for endpoints and payloads. Replace this stub with the real
    specification, or delete it and remove the references in 04-networking.md if this
    project has no HTTP API.
servers:
  - url: https://api.example.invalid
    description: FILL
paths:
  /health:
    get:
      summary: Liveness probe
      operationId: getHealth
      responses:
        "200":
          description: Service is reachable
          content:
            application/json:
              schema:
                type: object
                required: [status]
                properties:
                  status:
                    type: string
                    enum: [ok]
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
